function convertToLowerCase(inputElement) {
    // Obtém o valor do campo e converte para minúsculas
    var inputValue = inputElement.value.toLowerCase();

    // Atualiza o valor do campo para minúsculas
    inputElement.value = inputValue;
}
